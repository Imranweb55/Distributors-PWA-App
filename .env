# FILE: .env  (create this file yourself — copy this content into it)
# Distributors-PWA-App — local development
# Points the PWA at your LOCAL backend (localhost). Change to your Render
# backend URL for production the same way admin/.env does.
VITE_API_URL=http://localhost:5000